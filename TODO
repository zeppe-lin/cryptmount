TODO cryptmount

	---------------------------------------------------------------------
	Don't use getopt(1) instead of builtin "getopts" since the first
	utility is placed into /usr/bin and is unavailable at "separate /usr"
	boot.
	---------------------------------------------------------------------

	- [ ] The code refactoring is still a big goal.
	  - [ ] Fix shellcheck warnings (as part of code refactoring).

	- [ ] Add bash completion?

End of file.
